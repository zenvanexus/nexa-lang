Runnable embedding and language examples. See `docs/project-layout.md` §7.

Script examples use **`.lua`** (Lua-shaped source); minimal embedders use **`.zig`**. Conventions: **`docs/reference/source-files.md`**.
