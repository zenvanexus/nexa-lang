# Phase 0 — Tree-walking interpreter

This document will hold the full Phase 0 specification (syntax, `Value` / AST definitions, evaluation rules, and exit criteria).

Until that spec is authored here, use **`docs/project-layout.md` §14** for the Phase 0 bootstrap file list and **`src/`** for the evolving stub implementation.

For **`.lua` / `.nexa` vs `.zig`** (Lua+C-style hybrid), see **`docs/reference/source-files.md`**.
